<h1>Top Channels</h1>

<h3>Search by Name:</h3>
<%= form_tag(channels_path, method: :get) do%>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag ("Search") %>
<% end %>

<ul>
<%@data["data"].each do |instance|%>
<li>
  <%= instance["user_name"] %>
</li>
<% end %>
</ul>
