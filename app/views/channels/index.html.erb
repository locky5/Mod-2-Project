<h1>Top Channels</h1>

<h4>Search by Name:</h4>
<%= form_tag(channels_path, method: :get) do%>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag ("Search") %>
<% end %>
<br>
<%= link_to "Sort All Channels Alphabetically", channels_path(sort_by: "name") %>

<div class="container3">
  <ul>
    <%@channels_search.each do |channel|%>
    <li>
      <div class="bottom"><%=link_to channel.name, channel_path(channel)%><a href=<%=channel_path(channel)%>><img class="channel-box-art" src="<%=channel.box_art%>"alt="Smiley face"></a></div>
    </li>
    <% end %>
  </ul>
</div>
